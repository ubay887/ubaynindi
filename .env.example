# Wajib di production (Coolify → Environment Variables).
# Di `next dev` tanpa env ini, password lokal: ubay2026
# Tanpa env ini di production, login admin ditolak (fail-closed).
ADMIN_PASSWORD=

# Opsional. HMAC cookie admin. Jika kosong, memakai ADMIN_PASSWORD.
# ADMIN_SECRET=

# Domain publik, tanpa slash di akhir.
SITE_URL=https://ubaynindi.love

# Opsional. Default di Docker: /app/data
# DATA_DIR=
